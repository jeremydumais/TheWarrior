#pragma once

enum class SelectionMode
{
    Select,
    MoveMap,
    ApplyTexture,
    ApplyObject,
    EnableCanStep,
    DisableCanStep,
    ViewBorderMode,
    BlockBorderLeft,
    BlockBorderTop,
    BlockBorderRight,
    BlockBorderBottom,
    ClearBlockedBorders
};